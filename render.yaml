# ==============================================================================
# Render Blueprint Specification for Job Copilot
# ==============================================================================

services:
  # ----------------------------------------------------------------------------
  # 1. API Web Service (Phase 9.4/9.5 Cloud Deployment)
  # ----------------------------------------------------------------------------
  - type: web
    name: job-copilot-api
    runtime: docker
    dockerfilePath: Dockerfile
    plan: starter
    region: singapore
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: INFO
      - key: LOG_FORMAT
        value: json
      - key: HUMAN_CONFIRMATION_REQUIRED
        value: "true"
      - key: PORT
        value: "8000"
      - key: DATABASE_URL
        fromDatabase:
          name: job-copilot-postgres
          property: connectionString
      # ------------------------------------------------------------------------
      # Phase 12.2: Cloud Object Storage (Configure in Render Dashboard Environment)
      # - ARTIFACT_STORAGE_PROVIDER: s3
      # - S3_BUCKET: <your-r2-or-s3-bucket-name>
      # - S3_REGION: us-east-1 (or 'auto' for Cloudflare R2)
      # - S3_ENDPOINT_URL: https://<account_id>.r2.cloudflarestorage.com (if R2)
      # - S3_ACCESS_KEY_ID: <secret-access-key-id>
      # - S3_SECRET_ACCESS_KEY: <secret-access-key>
      # ------------------------------------------------------------------------

  # ----------------------------------------------------------------------------
  # 2. Scheduled Periodic Copilot Execution (Cron Job - Reserved for Future Phase)
  # NOTE: In Phase 9.4/9.5, only the Web API service is active.
  # ----------------------------------------------------------------------------
  # - type: cron
  #   name: job-copilot-periodic-discovery
  #   runtime: docker
  #   dockerfilePath: Dockerfile
  #   schedule: "0 */3 * * *"   # Runs every 3 hours
  #   dockerCommand: "python -m job_copilot copilot-discover && python -m job_copilot copilot-process"
  #   envVars:
  #     - key: APP_ENV
  #       value: production
  #     - key: LOG_LEVEL
  #       value: INFO
  #     - key: LOG_FORMAT
  #       value: json
  #     - key: HUMAN_CONFIRMATION_REQUIRED
  #       value: "true"
  #     - key: DATABASE_URL
  #       fromDatabase:
  #         name: job-copilot-postgres
  #         property: connectionString

# ------------------------------------------------------------------------------
# Managed PostgreSQL Database Foundation (Phase 9.5)
# ------------------------------------------------------------------------------
databases:
  - name: job-copilot-postgres
    databaseName: job_copilot
    user: job_copilot_user
    plan: starter
    region: singapore
